version: "3.7"
services:
  client:
    container_name: client
    build:
      context: ./client
    ports:
      - 3000:3000
      - 35729:35729
    environment:
      - NODE_ENV=development
    volumes:
      - './client/src:/app/src'
  server:
    container_name: server
    build:
      context: ./server
    ports:
      - 3001:3001
      - 9229:9229
    environment:
      - NODE_ENV=development
    volumes:
      - './server/src:/app/src'
